import { siteConfig } from "@/config/site";

export const loginPageConfig = {
    title: "Login",
    description: `Access ${siteConfig.name} to streamline your email tasks and boost productivity.`,
} as const;

