import { siteConfig } from "@/config/site";

export const signupPageConfig = {
    title: "Signup",
    description: `Join ${siteConfig.name} and let our bot handle your email tasks effortlessly.`,
} as const;
